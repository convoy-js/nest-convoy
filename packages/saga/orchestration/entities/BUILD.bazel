load("//tools:defaults.bzl", "nest_library")

nest_library(
    name = "entities",
    srcs = glob(
        ["*.ts"],
        exclude = ["*.spec.ts"],
    ),
    visibility = [
        "//packages/saga/orchestration:__pkg__",
    ],
    deps = [
        "@npm//typeorm",
    ],
)
