load("//tools:defaults.bzl", "nest_library")

nest_library(
    name = "entities",
    srcs = glob(
        ["*.ts"],
        exclude = ["*.spec.ts"],
    ),
    visibility = [
        "//packages/events/aggregate:__subpackages__",
    ],
    deps = [
        "@npm//typeorm",
    ],
)
