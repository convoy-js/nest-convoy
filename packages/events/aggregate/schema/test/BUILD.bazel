load("//tools:defaults.bzl", "nest_test")

nest_test(
    name = "test",
    srcs = glob(["*.ts"]),
    deps = [
        "//packages/events/aggregate",
        "@npm//@types/uuid",
        "@npm//@deepkit/type",
        "@npm//uuid"
    ],
)
