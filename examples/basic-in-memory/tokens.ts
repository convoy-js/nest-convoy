const uniqueId = Date.now();

export const COMMAND_CHANNEL = 'commandChannel-' + uniqueId;
export const COMMAND_DISPATCHER_ID = 'commandDispatcherId-' + uniqueId;
export const CUSTOMER_CHANNEL = 'customerChannel-' + uniqueId;
export const REPLY_CHANNEL = 'replyChannel-' + uniqueId;
