load("//tools:defaults.bzl", "nest_library")

nest_library(
    name = "common",
    srcs = glob(["**/*.ts"]),
    deps = [
        "//packages/sagas",
        "@npm//@nestjs/typeorm",
        "@npm//typeorm",
    ],
)
