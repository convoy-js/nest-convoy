load("//tools:defaults.bzl", "nest_library")

nest_library(
    name = "common",
    srcs = glob(["**/*.ts"]),
    deps = [
        "//packages/saga",
        "@npm//@nestjs/typeorm",
        "@npm//typeorm",
    ],
)
